# QA Report — BL-017 + BL-035

> **Task ID:** BL-017, BL-035  
> **Дата:** 2026-08-06  
> **QA:** независимая проверка (без опоры на отчёты разработчика)  
> **Commits:** `44cf5e9` (BL-017), `9acdf8b` (BL-035)  
> **Клиент:** `v0.1.1+25` (`connectFlow = tcp-underlay-v1`)

---

## 1. Объект проверки

| Задача | Описание | Заявленный статус |
|--------|----------|-------------------|
| **BL-017** | Fallback: UDP 443→8443→24443, затем TCP underlay 8443→24443 | Done |
| **BL-035** | Off-site backup: шифрование → `212.43.157.167`, cron 03:15 UTC, pull на PC | Done |

**APK:** `client/build/app/outputs/flutter-apk/StreamPass-v0.1.1+25-signed-arm64.apk` (11.7 MB)  
**OTA:** `https://212-43-156-33.nip.io/downloads/StreamPass.apk`

---

## 2. Изученные материалы

### Документация

- `docs/00_ProjectRules.md`, `02_TZ.md` §10, `03_CurrentState.md`, `04_Backlog.md`
- `docs/06_TestPlan.md`, `14_AIContext.md`, `15_DefinitionOfDone.md`, `16_AI_HANDOFF.md`
- `docs/17_CHANGELOG.md`, `27_BackupRecovery.md`, `33_DirectVsVpnBypass.md`, `99_ProjectDashboard.md`
- `ai/CurrentTask.md`, `ai/LastSession.md`

### Отчёты

- `reports/CodeReview/2c56972-1-review.md` — NEEDS FIX (старый коммит, не BL-017/035)
- Отдельного Code Review / Audit для `44cf5e9` / `9acdf8b` **не найдено**

### Код

- `client/go_core/internal/hyconfig/*` — fallback + TCP underlay
- `scripts/tcpunderlay/`, `scripts/setup-tcp-underlay.sh`
- `scripts/backup-offsite.sh`, `scripts/install-offsite-backup-cron.sh`, `scripts/PullBackupsOffsite.ps1`
- `scripts/VerifyDeviceE2E.ps1`

---

## 3. Соответствие ТЗ

### BL-017 — Fallback Strategy (ТЗ §10)

| Требование | Реализация | Статус |
|------------|------------|--------|
| UDP 443 → 8443 → 24443 | `FallbackCandidates()` | ✅ |
| TCP 443 | **Пропущен** (Caddy на 443) | ⚠️ отклонение |
| TCP 8443 | `tcpUnderlayConnFactory` + VPS bridge | ✅ |
| Автопереключение | `ConnectWithFallback()` в `tunnel.go` | ✅ |
| TCP 24443 | Добавлен сверх ТЗ (в ТЗ для TCP указан только 8443) | ℹ️ расширение |

**Prod-проверка TCP underlay:** порты **8443** и **24443** на `212.43.156.33` — **открыты** (`VerifyDeviceE2E.ps1`).

### BL-035 — Off-site backup

| Требование | Реализация | Статус |
|------------|------------|--------|
| Шифрование перед копированием | `openssl enc -aes-256-cbc -pbkdf2` | ✅ по коду |
| Второй хост | `OFFSITE_SSH` → `212.43.157.167` | ✅ по докам |
| Cron 03:15 UTC | `install-offsite-backup-cron.sh` | ⚠️ не проверен на VPS |
| Pull на PC | `PullBackupsOffsite.ps1` | ✅ скрипт есть |

---

## 4. Матрица проверок

### Выполнено (runtime)

| Проверка | Команда / метод | Результат |
|----------|-----------------|-----------|
| Backend unit + integration | `go test ./...` | ✅ **PASS** (~69 s) |
| Backend vet | `go vet ./...` | ✅ **PASS** |
| Backend build | `go build ./backend/cmd/server` | ✅ **PASS** |
| go_core hyconfig (BL-017) | `go test ./...` в `client/go_core` | ✅ hyconfig, dnscache, tunbridge — **PASS** |
| Smoke test (prod API) | `scripts/SmokeTest.ps1` | ✅ **8/8 PASS** |
| Device E2E API | `scripts/VerifyDeviceE2E.ps1` | ✅ **PASS** |
| Prod regions/config/rules | VerifyDeviceE2E | ✅ de, nl, pl, fi; config v4; rules=30 |
| OTA download URL | HEAD запрос | ✅ HTTP 200 |
| TCP underlay ports | VerifyDeviceE2E | ✅ :8443, :24443 open |
| APK на диске | VerifyDeviceE2E | ✅ +25 signed arm64 |
| adb install | VerifyDeviceE2E, device `RFGYB48SWAF` | ✅ Streamed Install Success |

### Выполнено (code-only)

| Проверка | Результат |
|----------|-----------|
| Архитектура BL-017 | Изменения изолированы в `go_core/hyconfig` + ops-скрипты |
| Unit-тесты fallback / framing | `fallback_test.go`, `tcp_underlay_test.go` — покрытие есть |
| Backlog / CHANGELOG [Unreleased] | BL-017/035 отмечены |
| `27_BackupRecovery.md` | Актуален для BL-035 |
| CI конфиг | `.github/workflows/ci.yml` — go test, flutter test, docker build |

### Не пройдено / не выполнено

| Проверка | Результат | Примечание |
|----------|-----------|------------|
| go_core mobile tests | ❌ **FAIL** | `TestDecideRoute_defaultRelay`: got DIRECT, want RELAY |
| Flutter tests | ❌ **1 FAIL** / 46 pass | `connect_flow_log_test.dart`: Binding not initialized |
| Flutter analyze | ⚠️ info only | deprecated `withOpacity`, `WillPopScope` — не блокер |
| Off-site backup на prod | ❌ не проверен | Нет SSH к VPS / secondary |
| Restore drill BL-035 | ❌ не выполнен | |
| Manual VPN connect на устройстве | ❌ не выполнен QA | APK установлен; connect flow не прогнан |
| TCP underlay handshake E2E | ❌ не выполнен | `STREAMPASS_RELAY_URI` integration test не запускался |
| Load test | ❌ не выполнен | |
| Code Review для `44cf5e9`/`9acdf8b` | ❌ отсутствует | |

---

## 5. Regression / связанные функции

| Область | Оценка |
|---------|--------|
| UDP connect + fallback | Код корректен; prod TCP bridge доступен |
| Decision Engine default | `DefaultMode = DIRECT` — тест `TestDecideRoute_defaultRelay` **устарел** (ожидает RELAY) |
| RU split DNS / bypass (+23) | Не scope BL-017; требует отдельного device-теста |
| Backend relay API | Не менялся в BL-017/035 |

---

## 6. Документация vs код

**Рассинхрон версии клиента (+25 vs +17/+23):**

| Документ | Указано | Факт |
|----------|---------|------|
| `pubspec.yaml`, `build_info.dart` | — | **+25** |
| `03_CurrentState.md` (заголовок / Prod APK) | +23 / **+17** | **+25** |
| `14_AIContext.md` | +23 | **+25** |
| `99_ProjectDashboard.md`, `01_Project.md`, `12_ReleasePlan.md` | +17 | **+25** |
| `17_CHANGELOG.md` [Unreleased] | +17 в Added | **+25** |

**Пропуски DoD:**

- `docs/10_Progress.md` — нет записей BL-017 / BL-035
- `06_TestPlan.md` — device E2E «+17», статусы без даты последнего прогона

---

## 7. Найденные проблемы

### Critical

| ID | Проблема | Рекомендация |
|----|----------|--------------|
| **C-01** | `go_core/mobile` test **FAIL** — блокирует `go test` в go_core | Обновить `TestDecideRoute_defaultRelay` под `DefaultMode = DIRECT` или исправить engine |
| **C-02** | Flutter test **FAIL** в `connect_flow_log_test.dart` | Добавить `TestWidgetsFlutterBinding.ensureInitialized()` в `main()` или mock `VpnChannel` |

### Major

| ID | Проблема | Рекомендация |
|----|----------|--------------|
| **M-01** | Документация не синхронизирована с **+25** | Documentation Engineer: единая версия во всех docs |
| **M-02** | **Off-site backup не верифицирован** на prod | Проверить cron, `offsite.log`, `.enc` на `212.43.157.167`; restore drill |
| **M-03** | **Manual device connect** не выполнен QA после install +25 | Connect → YouTube/`.ru` DIRECT → app bypass → disconnect |
| **M-04** | Отклонение от ТЗ §10: нет **TCP 443** | ADR в `11_Decisions.md` или реализация |
| **M-05** | Нет Code Review для BL-017/035 commits | Прогнать review перед release |

### Minor

| ID | Проблема | Рекомендация |
|----|----------|--------------|
| **m-01** | `ConnectResult.Candidate` не в логах/UI | Логировать `udp/443` / `tcp/8443` при успешном connect |
| **m-02** | `10_Progress.md` без BL-017/035 | Добавить записи 2026-08-06 |
| **m-03** | `06_TestPlan.md` устарел | Обновить матрицу и дату прогона |

### Info

| ID | Проблема | Рекомендация |
|----|----------|--------------|
| **I-01** | TCP 24443 добавлен сверх ТЗ | Задокументировать как расширение §10 |
| **I-02** | `openssl enc -pass pass:` — пароль в process list | Рассмотреть `-pass file:` или env pipe |
| **I-03** | `PullBackupsOffsite.ps1` — `-pw` в pscp | Использовать SSH key auth |

---

## 8. Логи прогонов (сокращённо)

### go test ./... (backend + shared)

```
ok  streampass/backend/internal/integrationtest  6.110s
ok  streampass/shared/config  0.517s
... (all packages green)
Exit code: 0
```

### go test client/go_core

```
ok  streampass/go_core/internal/hyconfig  3.092s
FAIL streampass/go_core/mobile  TestDecideRoute_defaultRelay: got "DIRECT" want RELAY
Exit code: 1
```

### flutter test

```
46 passed, 1 failed
Failing: client/test/connect_flow_log_test.dart — Binding has not yet been initialized
```

### SmokeTest.ps1

```
=== Results: 8 passed, 0 failed ===
```

### VerifyDeviceE2E.ps1

```
[PASS] regions: de, nl, pl, fi
[PASS] config version=4 min=0.1.0
[PASS] download URL HTTP 200
[PASS] rules count=30
[PASS] TCP underlay :8443 open
[PASS] TCP underlay :24443 open
[PASS] APK installed (device RFGYB48SWAF)
Device E2E API checks: PASS
```

---

## 9. Возврат разработчику

**Да** — до закрытия **C-01**, **C-02**, **M-02**, **M-03**.

BL-017 функционально подтверждён на уровне API + TCP ports + unit-тестов hyconfig.  
BL-035 подтверждён только по коду и документации скриптов.

---

## 10. Итоговый вердикт

# FAIL

**Причины:**

1. **Regression:** 2 failing tests (go_core mobile, flutter) — DoD §2 не выполнен.
2. **Off-site backup** на prod не верифицирован (M-02).
3. **Manual VPN connect** на устройстве после +25 не выполнен QA (M-03).
4. **Документация** расходится с продуктом (+17/+23 vs +25) (M-01).

**Условие PASS (передача Documentation Engineer):**

- Green `go test` в `client/go_core` и `flutter test`
- Подтверждение off-site cron + `.enc` на secondary (или явное принятие риска)
- Manual device connect на +25
- Синхронизация docs до v0.1.1+25

---

*Отчёт создан QA 2026-08-06. Runtime-проверки выполнены локально; off-site backup и полный device connect — не подтверждены.*
